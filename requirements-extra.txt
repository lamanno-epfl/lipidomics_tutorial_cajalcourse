# Extras on top of EUCLID's pinned dependencies (installed via `pip install -e EUCLID`).
# Keep these compatible with EUCLID's stack (xgboost 1.7.6, anndata 0.10.9, jax 0.4.35).

# pull MALDI-MSI data from METASPACE
metaspace2020==2.0.9

# interpretable ML: feature attributions for the gene -> lipid-change models
shap==0.46.0

# gene ontology enrichment for naming predictive gene programs
goatools==1.4.12
mygene==3.2.2

# Allen Common Coordinate Framework atlas access (region transfer); CPU, cached
allensdk==2.16.2
# brainglobe-atlasapi is a lighter alternative also used in the notebooks
brainglobe-atlasapi==2.0.7
